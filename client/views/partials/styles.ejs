<style>
  h1 {
    font-family: 'Brush Script MT', 'Brush Script Std', 'Lucida Calligraphy', 'Lucida Handwriting', 'Apple Chancery', cursive;
  }
  h2, h3, h4, h5, h6 {
    font-family: 'Poiret One', sans-serif;
    font-weight: bold;
  }

  html{scroll-behavior:smooth}

  .account-page-font {
    font-family: 'Poppins', sans-serif;
  }

  #header {
    height: 132px;
  }

  .nav-container {
    height: 132px;
    padding: 1em;
    padding-left: 2em;
    padding-right: 2em;
  }

  .nav-logo {
    position: absolute;
    top: 1em;
  }

  .nav-link-container {
    position: absolute;
    right: 4em;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  nav li {
    margin-left: 0.8em;
    margin-right: 0.8em;
  }

  .nav-link-container h5 {
    margin-left: 2em;
  }

  main {
    padding-top: 132px;
    margin-bottom: 5em;
  }

  .home-image {
    position: absolute;
    left: 0;
    z-index: -1;
  }

  .home {
    background: rgb(255, 255, 255, 0.5);
  }

  .text-center {
    text-align: center;
  }

  .about-profile {
    display: flex;
  }

  .about-profile-img  {
    background: url(../../public/images/about-profile.jpg);
    background-size: contain;
    background-repeat: no-repeat;
    min-width: 20%;
  }

  .about-profile div {
    padding: 1em;
  }

  .jumbotron img {
    margin: 1em;
  }

  @media only screen and (max-width: 600px) {
    .about-profile {
      flex-direction: column;
    }

    .jumbotron img {
      margin: 0.2em;
    }
  }

  .about-image-grid {
    display: flex;
  }

  .div-sizing {
    margin: 0.5em;
    padding: 0.5em;
    height: 15em;
    width: 15em;
  }

  .mobile-display {
    display: none;
  }

  @media only screen and (max-width: 850px) {
    .footer-link-text {
      display: none;
    }
  }

  @media only screen and (max-width: 680px) {
    #footer-location-text {
      display: none;
    }
  }

  @media only screen and (max-width: 950px) {
    .nav-title {
      display: none;
    }
  }

  @media only screen and (max-width: 1400px) {
    .mobile-display {
      display: block;
    }
  }

  @media only screen and (max-width: 600px) {
    .div-sizing {
      max-width: 100%;
    }

    .card-text {
      font-size: 2.5rem;
    }

    .desktop-display {
      display: none;
    }
  }

  .class-type-list-div {
    display: flex;
    justify-content: center;
    flex-direction: column;
    max-width: 50%;
  }

  .card-center {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-direction: column;
  }

  .account-card-center {
    display: flex;
    flex-direction: column;
  }

  .admin-card p, h5 {
    margin-bottom: 0;
  }

  #admin-view-students-button {
    margin-top: 1em;
  }

  .portfolio-item {
    background-repeat: no-repeat;
    background-size: cover;
    border-radius: 10px;
    box-shadow: 2px 2px 5px rgb(117, 117, 117);
    transition: all 0.1s ease-in-out 0s;
    height: inherit;
    width: inherit;
  }

  .portfolio-item:hover {
    box-shadow: 5px 5px 8px rgb(73, 73, 73);
  }

  .portfolio-info {
    height: 100%;
    padding: 1em;
    color: rgb(40, 40, 40);
  }

  .testimonials {
    display: flex;
    flex-wrap: wrap;
  }

  .student-buttons {
    width: 80%;
    justify-content: space-around;
  }

  .fix-to-bottom {
    position: absolute;
    bottom: 1em;
    margin: 0;
  }

  .fixed-bottom {
    z-index: 999;
    margin-top: 10em;
  }

  .fixed-to-bottom {
    margin-top: auto;
  }

  .link-btn {
    background: #3498db;
    border: 0;
    padding: 10px 24px;
    color: #fff;
    transition: 0.4s;
    border-radius: 4px;
  }

  .closed-class {
    background: rgb(255, 143, 143);
  }

  .open-class {
    background: rgb(201, 254, 254);
  }

  button[type=submit] {
    background: #3498db;
    border: 0;
    padding: 10px 24px;
    color: #fff;
    transition: 0.4s;
    border-radius: 4px;
  }

  .btn-link {
    background: none!important;
    color: #3498db!important;
  }

  #card-error {
    color: rgb(105, 115, 134);
    text-align: left;
    font-size: 13px;
    line-height: 17px;
    margin-top: 12px;
  }

  #card-element {
    border-radius: 4px 4px 0 0 ;
    padding: 12px;
    border: 1px solid rgba(50, 50, 93, 0.1);
    height: 44px;
    width: 100%;
    background: white;
  }

  .account-box {
    display: flex;
    justify-content: space-between;
  }

  #sub-box-div {
    width: 30%;
  }
  @media only screen and (max-width: 600px) {
    .account-box {
      flex-direction: column;
    }
    
    #sub-box-div {
      width: 100%;
    }
  }

  .sub-box {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin: 0.2em;
    padding: 0.5em;
    border: 0.2px green solid;
    border-radius: 10px;
    background-color: #a5ffa5;
    box-shadow: 1px 1px 3px rgb(117, 117, 117);
    transition: all 0.1s ease-in-out 0s;
  }

  .sub-box:hover {
    box-shadow: 2px 2px 5px rgb(69, 69, 69);
  }

  .fine-print {
    margin: 2em;
    text-align: center;
  }

  /* Popup container */
  .popup {
    position: relative;
    display: inline-block;
    cursor: pointer;
  }

  /* The actual popup (appears on top) */
  .popuptext {
    display: none;
    width: 160px;
    background-color: #555;
    color: #fff;
    text-align: center;
    border-radius: 6px;
    padding: 8px 0;
    position: absolute;
    z-index: 1;
    margin-left: 77px;
    margin-top: 115px;
  }

  #cancel-form {
    margin-top: auto;
  }

  /* Popup arrow */
  .popuptext::after {
    content: "";
    position: absolute;
    top: -10px;
    left: 50%;
    margin-left: -5px;
    border-width: 5px;
    border-style: solid;
    border-color: transparent transparent #555 transparent;
  }

  .popuptext ul {
    padding: 0;
    margin-bottom: 0;
  }

  footer {
    padding: 0;
  }

  .footer-container {
    background-color: white;
    justify-content: space-around;
  }

  .footer-link {
    margin: 0.5em;
  }

  .dev-error {
    display: none;
  }

  /* Retreat class tiles */
  .tile-info {
    display: flex;
    align-items: center;
    flex-direction: column;
  }

  .tile-info p {
    margin-top: 5px;
    margin-bottom: 5px;
  }

  .retreat-class-label {
    display: flex;
    align-items: center;
    flex-direction: row;
    padding: 5px;
  }

  .retreat-class-checkbox {
    appearance: none;
    background: #fff;
    margin: 0;
    font: inherit;
    color: currentColor;
    width: 1.15em;
    height: 1.15em;
    border: 0.15em solid currentColor;
    border-radius: 0.15em;
    transform: translateY(-0.075em);
    display: grid;
    place-content: center;
    margin-right: 5px;
  }

  .retreat-class-checkbox::before {
    content: "";
    width: 0.65em;
    height: 0.65em;
    transform: scale(0);
    transition: 120ms transform ease-in-out;
    box-shadow: inset 1em 1em rgb(31, 136, 36);
  }

  .retreat-class-checkbox:checked::before {
    transform: scale(1);
  }

  .retreat-info-button {
    background: #3498db;
    border: 0;
    font-size: small;
    padding: 5px 10px;
    color: #fff;
    transition: 0.4s;
    border-radius: 4px;
  }

  #retreat-submit {
    display: flex;
    justify-content: center;
    margin-top: 20px;
  }

  #retreat-submit button {
    font-size: large;
    padding: 20px 48px;
  }

</style>
