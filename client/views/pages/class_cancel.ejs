<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head'); %>
  </head>
  <body class="container">

    <header>
      <%- include('../partials/header'); %>
    </header>

    <main>
      <div class="jumbotron">
        <% if (credits > 1) { %>
          <p>Cancelling this class will refund you <%= credits %> credits. Are you sure?</p>
        <% } else { %>
          <p>Cancelling this class will refund you <%= credits %> credit. Are you sure?</p>
        <% } %>
        <p>You have <%= user.credits %> credits in your account.</p>
        <form action="/account" method="GET">
          <button type="submit">Cancel</button>
        </form>
        <form action="/schedule/cancel" method="POST">
          <input name="class_id" value=<%= class_id %> hidden />
          <input name="credits" value=<%= credits %> hidden />
          <button type="submit">Confirm</button>
        </form>
      </div>
    </main>

    <footer>
      <%- include('../partials/footer'); %>
    </footer>

  </body>
</html>